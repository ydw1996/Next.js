import { Box, Button } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <Box className="bg-black h-screen w-full p-4">
      <Button variant="contained"></Button>
      <Box className="text-black">리스트</Box>
    </Box>
  );
};

export default page;
